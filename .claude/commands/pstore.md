Restore gitignored personal files (personal skills + CLAUDE.md + CLAUDE.local.md) from the vault backup. Use after cloning on a new instance, or after losing local state.

```bash
bash bin/pstore.sh
```

Scope: same as `pbackup` — personal skills + CLAUDE.md / CLAUDE.local.md.
