node_modules/
data/
credentials/
mcp-credentials/
~
.env
.DS_Store
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json
.mcp.local.json
Obsidian

# CLAUDE.local.md — optional personal overrides, not committed.
# CLAUDE.md is now tracked in git (generic, no personal data).
# CRON.md lives directly at <vault>/persona/CRON.md; no repo-root copy.
CLAUDE.local.md

# Skills: most are personal/vault-backed and stay out of git. Track only
# the shared ones that should ship with the repo.
.claude/skills/*
!.claude/skills/assistant-loop/
!.claude/skills/assistant-test/
!.claude/skills/kb/
!.claude/skills/onboarding

# Editor swap files
*.swp
*.swo
